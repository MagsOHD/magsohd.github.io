name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    environment: github-pages
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Inject Web3Forms key via HTML meta tags
        run: |
          echo "🔧 INJECTION Web3Forms via META TAGS"

          # Test si les fichiers existent
          ls -la currentFolio/*.html

          # Injecter dans tous les fichiers HTML avec une méthode plus robuste
          for file in currentFolio/*.html; do
            if [ -f "$file" ]; then
              echo "=== Traitement de $file ==="
              echo "Contenu avant injection (premières lignes):"
              head -10 "$file"

              # Créer un fichier temporaire avec l'injection
              awk -v key="${{ secrets.WEB3FORMSKEY }}" '
              /<head>/ {
                print $0
                print "    <meta name=\"web3forms-key\" content=\"" key "\">"
                next
              }
              { print }
              ' "$file" > "$file.tmp"

              # Remplacer le fichier original
              mv "$file.tmp" "$file"

              echo "Contenu après injection (premières lignes):"
              head -10 "$file"
              echo "✅ Meta tag ajoutée à $file"
            fi
          done

          echo "=== VÉRIFICATION FINALE ==="
          if grep -r "web3forms-key" currentFolio/*.html; then
            echo "✅ Meta tags trouvées dans les fichiers HTML"
          else
            echo "❌ ERREUR: Aucune meta tag trouvée - ARRÊT"
            exit 1
          fi

          echo "✅ Injection terminée avec succès"
        env:
          WEB3FORMSKEY: ${{ secrets.WEB3FORMSKEY }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
