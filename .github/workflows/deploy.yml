jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run app with secret
        run: node index.js
        env:
          github-pages: ${{ secrets.WEB3FORMSKEY }}
