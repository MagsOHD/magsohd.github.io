name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    environment: github-pages
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Inject ntfy.sh topic via HTML meta tags and JS config
        run: |
          echo "🔔 INJECTION ntfy.sh topic via META TAGS et CONFIG JS"

          # Vérifier si le secret NTFY_TOPIC est défini
          if [ -z "${{ secrets.NTFY_TOPIC }}" ]; then
            echo "⚠️ ATTENTION: Secret NTFY_TOPIC non défini - utilisation du fallback"
            NTFY_TOPIC_VALUE="lucas-portfolio-fallback-$(date +%s)"
          else
            echo "✅ Secret NTFY_TOPIC trouvé"
            NTFY_TOPIC_VALUE="${{ secrets.NTFY_TOPIC }}"
          fi

          echo "Topic à utiliser: $NTFY_TOPIC_VALUE"

          # Test si les fichiers existent
          ls -la currentFolio/*.html

          # Injecter dans tous les fichiers HTML avec une méthode plus robuste
          for file in currentFolio/*.html; do
            if [ -f "$file" ]; then
              echo "=== Traitement de $file ==="
              echo "Contenu avant injection (premières lignes):"
              head -10 "$file"

              # Créer un fichier temporaire avec l'injection du meta tag ntfy
              awk -v topic="$NTFY_TOPIC_VALUE" '
              /<head>/ {
                print $0
                print "    <meta name=\"ntfy-topic\" content=\"" topic "\">"
                next
              }
              { print }
              ' "$file" > "$file.tmp"

              # Remplacer le fichier original
              mv "$file.tmp" "$file"

              echo "Contenu après injection (premières lignes):"
              head -10 "$file"
              echo "✅ Meta tag ntfy ajoutée à $file"
            fi
          done

          echo "=== INJECTION DANS LE FICHIER CONFIG JS ==="
          if [ -f "currentFolio/ntfy-config.js" ]; then
            echo "Contenu avant injection:"
            cat currentFolio/ntfy-config.js

            # Remplacer différents patterns pour le topic
            sed -i "s|lucas-portfolio-dev-[^']*|$NTFY_TOPIC_VALUE|g" currentFolio/ntfy-config.js
            sed -i "s/YOUR_NTFY_TOPIC/$NTFY_TOPIC_VALUE/g" currentFolio/ntfy-config.js

            echo "Contenu après injection:"
            cat currentFolio/ntfy-config.js
            echo "✅ Topic ntfy.sh injecté dans ntfy-config.js"
          else
            echo "❌ ATTENTION: ntfy-config.js non trouvé"
          fi

          echo "=== VÉRIFICATION FINALE ==="
          if grep -r "ntfy-topic" currentFolio/*.html; then
            echo "✅ Meta tags ntfy trouvées dans les fichiers HTML"
          else
            echo "⚠️ WARNING: Aucune meta tag ntfy trouvée"
          fi

          if grep -r "$NTFY_TOPIC_VALUE" currentFolio/ntfy-config.js; then
            echo "✅ Topic ntfy.sh configuré dans le fichier JS"
          else
            echo "⚠️ WARNING: Topic ntfy.sh non configuré dans le JS"
          fi

          echo "✅ Injection ntfy.sh terminée avec succès"
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
